{% extends 'base.html.twig' %}

{% block title %}Hotel registration{% endblock %}

{% block body %}
<h3 class="text-center"> Créer votre compte facilement</h3>
<div class="container  mt-5 mb-5">
    <div class="row">
        <div class="col-6 mx-auto bg-light pe-5 pt-5 pl-5 pb-5">
            {{ form_start(form)}}

            {{form_widget(form)}}

            <button type="submit" class="btn btn-primary w-100" > Valider </button>
            {{form_end(form)}}

            <p class="mt-2 mb-2"> Vous avez déjà un compte ?<a href="{{path('app_login')}}" class="text-decoration-none"> Connectez vous</a> </p>
        </div>
    </div>
</div>
{% endblock %}
